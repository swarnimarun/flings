/// non script programs can use main function
[<EntryPoint>]
let main =
    // TODO: provide argv parameter(unused parameters can be represented with '_')
    // and return '0' integer from the main function
    printfn "I am let!"
